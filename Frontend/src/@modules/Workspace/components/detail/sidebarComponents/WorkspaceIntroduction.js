import { Card, CardBody, CardHeader, CardTitle } from "reactstrap"

const WorkspaceIntroduction = (props) => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Introduction</CardTitle>
      </CardHeader>
      <CardBody>sidebar</CardBody>
    </Card>
  )
}

export default WorkspaceIntroduction
